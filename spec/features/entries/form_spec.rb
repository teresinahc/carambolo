require "rails_helper"

RSpec.describe 'form', type: :feature do
  describe 'submit a form' do
    it "redirect to" do
      # expect(subject).to redirect_to("/entries/:id(.:format)")
    end

    it "renders" do
      # expect(response).to render_template(:index)
    end
  end
end
